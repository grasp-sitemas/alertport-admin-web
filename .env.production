# Committed defaults for the `next build` production target. Vercel's
# Project Environment Variables (Production scope) take precedence at
# build time on the platform; these are the fallback for local
# `vercel --prod` or CI runs without an env pull.
#
# IMPORTANT: NEXT_PUBLIC_* vars are embedded in the client bundle.
# Do NOT put real secrets here (TURN passwords, private keys, etc.) —
# configure them per-deployment in your host's secret manager.

# ───────────── API ─────────────
NEXT_PUBLIC_API_URL=https://api-gateway-shield.herokuapp.com

# ───────────── WebSocket / Chat (ms-chat) ─────────────
NEXT_PUBLIC_MS_CHAT_URL=https://ms-chat-shield.herokuapp.com

# ───────────── WebRTC ─────────────
# Production should use hosted TURN. Fill via host secret manager:
NEXT_PUBLIC_WEBRTC_ICE_SERVERS=
NEXT_PUBLIC_WEBRTC_TURN_URLS=
NEXT_PUBLIC_WEBRTC_TURN_USERNAME=
NEXT_PUBLIC_WEBRTC_TURN_PASSWORD=

# ───────────── Firebase (PRD) ─────────────
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyAdcAjt9Cdjs4vh5BTxSXcWbT0uchMcM-A
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=shieldgo.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=shieldgo
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=shieldgo.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=569460808844
NEXT_PUBLIC_FIREBASE_APP_ID=1:569460808844:web:14156a28a547faf8926fa9
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-SJYL759PFQ

# ───────────── App ─────────────
NEXT_PUBLIC_APP_MODE=production
NEXT_PUBLIC_IS_PRODUCTION=true
NEXT_PUBLIC_DEFAULT_LOCALE=pt
NEXT_PUBLIC_MASTER_ADMIN_EMAILS=

# ───────────── Observability ─────────────
# Sentry project: alerport-admin-web (ID 4511260921430016)
# Events go through the same-origin `/monitoring` tunnel, so the DSN
# only needs to be valid for browser SDK boot - no CSP allow-list.
NEXT_PUBLIC_SENTRY_DSN=https://fa18e4af35e84132fbe13d55e0476b33@o4507014507986944.ingest.us.sentry.io/4511260921430016
# Environment tag attached to every Sentry event. Decouples the
# Sentry HML/PROD separator from NEXT_PUBLIC_APP_MODE so feature
# flags that key on APP_MODE keep their existing values.
NEXT_PUBLIC_SENTRY_ENVIRONMENT=production
# Selects the target project when the build uploads source maps.
SENTRY_ORG=grasp-jc
SENTRY_PROJECT=alerport-admin-web
