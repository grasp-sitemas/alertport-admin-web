'use client';

// Re-exports the shared hierarchy lookups so existing imports keep working.
export {
  useClientsLookup,
  useAccountsLookup,
  useSitesLookup,
} from '@/features/shared/use-hierarchy-lookups';
